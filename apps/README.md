# apps

Thin entrypoints only.

Planned apps:

- `cli`: terminal-first interactive workbench
- `gateway`: multi-channel message ingress and delivery
- `control-plane`: thread, task, approval, and run visibility
